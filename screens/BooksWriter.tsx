import { ScrollView, StyleSheet, Text, View } from 'react-native'
import React from 'react'

const BooksWriter = () => {
  return (
    <ScrollView style={{height: '100%',  backgroundColor: '#f6f8fa'}}>
      <Text>AboutApp</Text>
    </ScrollView>
  )
}

export default BooksWriter

const styles = StyleSheet.create({})